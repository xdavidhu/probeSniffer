language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
install: "sudo apt update && sudo apt install tshark -y && sudo python3 -m pip install -r requirements.txt"
script: sudo ./probeSniffer.py -h
